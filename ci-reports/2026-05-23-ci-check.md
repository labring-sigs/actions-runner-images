# CI Check Report - 2026-05-23

## Summary

- Repository: `labring-sigs/actions-runner-images`
- Checked at: `2026-05-23 09:03:30 CST`
- Checked branch: `main`
- Local HEAD: `1f66542a14929cfa2f701a2ac7f9ed6994eec021`
- Result: GitHub Actions could not be queried from this environment. Local static checks passed, and no code fix was applied without remote CI failure evidence.

## Remote CI status

The latest GitHub Actions runs could not be checked because the local execution environment cannot reach GitHub during this run:

| Check | Result |
| --- | --- |
| `git fetch origin main --prune` | Failed: SSH connection to `ssh.github.com:443` is not permitted |
| `git ls-remote https://github.com/labring-sigs/actions-runner-images.git refs/heads/main` | Failed: `Could not resolve host: github.com` |
| `curl https://api.github.com/repos/labring-sigs/actions-runner-images/actions/runs?per_page=10` | Failed: `Could not resolve host: api.github.com` |

Because the remote CI logs were unavailable, there was no observable CI error to diagnose or fix in this run.

## Local verification

The following checks were run against the local `main` checkout:

| Check | Result |
| --- | --- |
| `bash -n INSTALL_skopeo.sh` | Passed |
| Parse `.github/workflows/release.yml` as YAML | Passed |
| `git diff --check` | Passed |

## Notes

The previous successful run on `2026-05-22` confirmed the latest available GitHub Actions checks were passing and pushed report commit `1f66542a14929cfa2f701a2ac7f9ed6994eec021` to `main`.

This report records the current automation attempt and should be updated or superseded after GitHub network access is restored and the latest workflow runs can be queried.

## Push status

The report was committed locally as part of commit `05df090`, but five push attempts failed with the same network restriction:

```text
ssh: connect to host ssh.github.com port 443: Operation not permitted
fatal: Could not read from remote repository.
```

The local `main` branch remains ahead of `origin/main` until the environment can reach GitHub.
