# CI Check Report - 2026-05-25

## Summary

- Repository: `labring-sigs/actions-runner-images`
- Checked at: `2026-05-25 09:09:00 CST`
- Checked branch: `main`
- Local HEAD before this report: `d839c66083b38d66777b17f4c65133943844f9ee`
- Last known `origin/main`: `1f66542a14929cfa2f701a2ac7f9ed6994eec021`
- Result: GitHub Actions could not be queried from this environment. No new CI failure evidence was available, so no code fix was applied.

## Remote CI status

The latest GitHub Actions runs could not be checked from this environment.
The first check ran at `2026-05-25 01:35:40 CST`; this report was refreshed
at `2026-05-25 09:09:00 CST` with the same remote-access result:

| Check | Result |
| --- | --- |
| `curl https://api.github.com/repos/labring-sigs/actions-runner-images/actions/runs?branch=main&per_page=20` | Failed: `Could not resolve host: api.github.com` |
| `curl https://api.github.com/repos/labring-sigs/actions-runner-images/actions/runs?per_page=10` | Failed: `Could not resolve host: api.github.com` |
| `git fetch origin main --prune` | Failed: SSH connection to `ssh.github.com:443` is not permitted |
| `git ls-remote https://github.com/labring-sigs/actions-runner-images.git refs/heads/main` | Failed: `Could not resolve host: github.com` |

Because the remote CI logs were unavailable, there was no observable CI error to diagnose. The automation therefore preserved the current code and recorded the local verification result.

## Local verification

The following checks were run against the local `main` checkout:

| Check | Result |
| --- | --- |
| `bash -n INSTALL_skopeo.sh` | Passed |
| Parse `.github/workflows/release.yml` as YAML with Ruby | Passed |
| `git diff --check` | Passed |
| `docker buildx version` | Not run: `docker` is not installed in this environment |

## Notes

The local branch was already ahead of `origin/main` by two report commits from
`2026-05-23` and `2026-05-24` at the start of this run. This report adds the
current automation result and should be superseded by a CI-log-based fix if
GitHub Actions logs become reachable.

## Push status

The report was committed locally. Pushing to `main` was retried 10 times during
the first check and 20 more times during the 09:09 CST refresh. Every attempt
failed with the same sandbox-level SSH network error:

```text
ssh: connect to host ssh.github.com port 443: Operation not permitted
fatal: Could not read from remote repository.
```

The local `main` branch remains ahead of `origin/main` by three commits:

- `d386824` - `ci: add 2026-05-23 status report`
- `d839c66` - `ci: add 2026-05-24 status report`
- current report commit - `ci: add 2026-05-25 status report`
